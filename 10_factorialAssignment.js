var facorialOfNum = function(num){
//  function facorialOfNum(num){
    if (num == null || num == undefined) {
        console.log(`${num} is the Invalid Input`);
        return;
    }
    let fact = 1;
    for (let i = 5; i >= 1; i--) {
        fact = fact * i 
    }
    console.log(`Factorial of 5 is : ${fact}`);
}
facorialOfNum(5);

var facorialOfNum = function(num){
    //  function facorialOfNum(num){
        if (num == null || num == undefined) {
            console.log(`${num} is the Invalid Input`);
            return;
        }
        let fact = 1;
        for (let i = 3; i >= 1; i--) {
            fact = fact * i 
        }
        console.log(`Factorial of 3 is : ${fact}`);

    }
facorialOfNum(3);

var facorialOfNum = function(num){
    //  function facorialOfNum(num){
        if (num == null || num == undefined) {
            console.log(`${num} is the Invalid Input`);
            return;
        }
        let fact = 1;
        for (let i = 3; i >= 1; i--) {
            fact = fact * i 
        }
        console.log(`Factorial of null is : ${fact}`);

    }
facorialOfNum(null);

var facorialOfNum = function(num){
    //  function facorialOfNum(num){
        if (num == null || num == undefined) {
            console.log(`${num} is the Invalid Input`);
            return;
        }
        let fact = 1;
        for (let i = 8; i >= 1; i--) {
            fact = fact * i 
        }
        console.log(`Factorial of 8 is : ${fact}`);

    }
facorialOfNum(8);

var facorialOfNum = function(num){
    //  function facorialOfNum(num){
        if (num == null || num == undefined) {
            console.log(`${num} is the Invalid Input`);
            return;
        }
        let fact = 1;
        for (let i = 3; i >= 1; i--) {
            fact = fact * i 
        }
        console.log(`Factorial of undefined is : ${fact}`);

    }
facorialOfNum(undefined);

var facorialOfNum = function(num){
    //  function facorialOfNum(num){
        if (num == null || num == undefined) {
            console.log(`${num} is the Invalid Input`);
            return;
        }
        let fact = 1;
        for (let i = 9; i >= 1; i--) {
            fact = fact * i 
        }
        console.log(`Factorial of 9 is : ${fact}`);

    }
facorialOfNum(9);

var facorialOfNum = function(num){
    //  function facorialOfNum(num){
        if (num == null || num == undefined) {
            console.log(`${num} is the Invalid Input`);
            return;
        }
        let fact = 1;
        for (let i = 0; i >= 1; i--) {
            fact = fact * i 
        }
        console.log(`Factorial of 0 is : ${fact}`);

    }
facorialOfNum(0);

